FROM alpine:3.20

RUN apk add --no-cache curl bash coreutils ca-certificates

WORKDIR /opt/meshagent
COPY scripts/run-meshagent-container.sh /usr/local/bin/run-meshagent-container.sh
RUN chmod +x /usr/local/bin/run-meshagent-container.sh

ENTRYPOINT ["/usr/local/bin/run-meshagent-container.sh"]
