version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: remote-mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    networks:
      - remote-net

  meshcentral:
    image: ylianst/meshcentral:latest
    container_name: remote-meshcentral
    restart: unless-stopped
    depends_on:
      - mongo
    ports:
      - "80:80"
      - "443:443"
    environment:
      - HOSTNAME=your-public-hostname-or-ip
      - REVERSE_PROXY=false
      - IFRAME=false
      - ALLOW_NEW_ACCOUNTS=false
      - WEBRTC=true
      - SESSION_KEY=change-this-session-key
      - MONGO_URL=mongodb://mongo:27017/meshcentral
    volumes:
      - ./meshcentral-data:/opt/meshcentral/meshcentral-data
      - meshcentral-userfiles:/opt/meshcentral/meshcentral-files
      - meshcentral-backup:/opt/meshcentral/meshcentral-backups
    networks:
      - remote-net

networks:
  remote-net:
    driver: bridge

volumes:
  mongo-data:
  meshcentral-userfiles:
  meshcentral-backup:
